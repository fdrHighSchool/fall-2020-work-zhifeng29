take each line from a file and turn it into a string
use substring to locate the 2 number 
ex: int min= Integer.parseInt (input.substring(0,string.indexOf("-")))
int max= Integer.parseInt (input.substring(string.indexOf("-"+1,string.indexOf(" ")))
locate the letter you going to find then set it to variable to compare to later
locate":" start compare after that 
compare substring of each letter to variable if it is in range of min and max 
ex: _______________
while something.hasnext(){
{
for(i = ":"index;i < length of string you got ;i++){
    if (input.substring(string.indexOf(i),string.indexOf(i+1){
     int count++;
    }
}
if (count >= min and <= max) {
correctpassword++
}
}
}
__________________
the correct password count go up my one
go to next string until no nextline left